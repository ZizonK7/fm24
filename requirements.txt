# 핵심 파이프라인은 표준 라이브러리만으로 동작한다.
# 아래는 전부 선택 사항이며, 설치돼 있으면 자동으로 쓰인다.

# HTML 파싱 가속. 없으면 표준 html.parser를 쓴다 (결과는 동일).
lxml>=5.0

# 노트북/임시 분석용. 파이프라인 자체는 pandas를 쓰지 않는다.
pandas>=2.0

# 테스트. 없으면 python -m unittest discover -s tests -t . 로 실행 가능.
pytest>=8.0
